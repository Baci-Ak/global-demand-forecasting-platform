# ============================================================
# Airflow task runtime dependencies (local dev + production-like)
#
# Principles
# - Pin versions to match repo requirements where possible.
# - Include only what DAG tasks need (ingestion + DQ + warehouse loaders).
# - EXCLUDE dbt to avoid protobuf/version conflicts and keep orchestration slim.
# - EXCLUDE API-only deps (fastapi/uvicorn) - not needed in Airflow workers.
# ============================================================

# ----------------------------
# Config
# ----------------------------
python-dotenv==1.0.1
pydantic-settings==2.12.0
pydantic==2.12.5

# ----------------------------
# Database / migrations (tasks write audit logs)
# ----------------------------
SQLAlchemy
psycopg2-binary==2.9.11

# ----------------------------
# AWS/S3 + ingestion
# ----------------------------
boto3==1.34.162
kaggle

# ----------------------------
# Data processing
# ----------------------------
pandas==2.3.3
pyarrow==15.0.2
tqdm

# ----------------------------
# Data quality
# ----------------------------
great_expectations==1.11.0


# ----------------------------
# dbt (if we run dbt commands from Airflow)
# ----------------------------
dbt-core==1.11.2
dbt-redshift==1.10.0